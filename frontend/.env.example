# QUORIXA Frontend environment variables
# Copy this file to `.env` and adjust as needed. Never commit secrets here.

# Base URL of the QUORIXA backend API (without /api/v1 path suffix issues).
# Defaults to http://localhost:5000/api/v1 when unset.
VITE_API_URL=http://localhost:5000/api/v1